name: Debug - Reinitialise ADMIN_EMAILS avec les comptes reels

on:
  workflow_dispatch:

jobs:
  reset:
    runs-on: ubuntu-latest
    steps:
      - uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Definir ADMIN_EMAILS avec les 3 comptes admin confirmes
        run: |
          supabase secrets set ADMIN_EMAILS="admin@gesta.fr,esthor.pl@gmail.com,bonjour@gesta-tapisserie.com" --project-ref dekmujrzeverlmnzjzbh
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
